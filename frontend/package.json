{
  "name": "frontend-dev",
  "version": "1.0.3",
  "description": "Frontend Admin para WhatsApp Agent",
  "type": "module",
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "build": "bun build src/index.ts --outdir ../backend/public --target browser",
    "start": "echo \"Frontend serves from backend\""
  },
  "dependencies": {},
  "devDependencies": {
    "typescript": "^5.5.0",
    "bun-types": "^1.0.0"
  }
}
